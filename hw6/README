# README
## Solving a 3D Truss
This code will take inputs of nodes, displacements, forces and elements and return the values for the displacement of each node, and the forces in each element.

### How to run the code:
To run the code you will need to execute this command:
```
python3 main.py
```
Input files are included and should remain in the same path as the python script

### Necessary modules:
numpy, scipy, matplotlib, and math